<% content_for :title, "Grupos · Dokivo" %>

<div class="space-y-8">
  <header class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between">
    <div>
      <p class="text-xs font-semibold uppercase tracking-wide text-accent">Organização</p>
      <h1 class="mt-1 text-2xl font-semibold tracking-tight text-zinc-900 sm:text-3xl">Grupos</h1>
      <p class="mt-2 max-w-2xl text-sm leading-relaxed text-zinc-600">
        Agrupe usuários ou permissões por conta.
      </p>
    </div>
    <%= link_to "Novo grupo", new_group_path, class: "app-btn-primary w-full shrink-0 sm:w-auto" %>
  </header>

  <% if @groups.any? %>
    <div class="app-card overflow-hidden">
      <div class="overflow-x-auto">
        <table class="min-w-full text-left text-sm">
          <thead>
            <tr class="border-b border-zinc-200 bg-zinc-50/90 text-xs font-semibold uppercase tracking-wide text-zinc-500">
              <th class="px-4 py-3 font-semibold">Nome</th>
              <th class="hidden px-4 py-3 font-semibold sm:table-cell">Conta</th>
              <th class="px-4 py-3 text-right font-semibold">Ações</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-zinc-100">
            <%= render partial: "groups/group_row", collection: @groups, as: :group %>
          </tbody>
        </table>
      </div>
    </div>
  <% else %>
    <div class="app-card px-6 py-16 text-center">
      <p class="text-sm font-medium text-zinc-900">Nenhum grupo cadastrado</p>
      <p class="mt-1 text-sm text-zinc-500">Crie o primeiro grupo para organizar o acesso.</p>
      <%= link_to "Novo grupo", new_group_path, class: "app-btn-primary mt-6 inline-flex" %>
    </div>
  <% end %>
</div>

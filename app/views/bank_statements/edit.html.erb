<% content_for :title, "Editar lançamento · #{current_client.name} · Dokivo" %>

<div class="mx-auto max-w-3xl space-y-6">
  <header>
    <h1 class="text-2xl font-semibold tracking-tight text-zinc-900 sm:text-3xl">Editar lançamento</h1>
    <p class="mt-2 text-sm text-zinc-600">Atualize os dados do lançamento selecionado.</p>
  </header>

  <section class="app-card p-6 sm:p-8">
    <%= render "form", bank_statement: @bank_statement %>
  </section>
</div>

<% content_for :title, "Novo lançamento · #{current_client.name} · Dokivo" %>

<div class="mx-auto max-w-3xl space-y-6">
  <header>
    <h1 class="text-2xl font-semibold tracking-tight text-zinc-900 sm:text-3xl">Novo lançamento</h1>
    <p class="mt-2 text-sm text-zinc-600">Registre um lançamento manualmente no extrato do cliente ativo.</p>
  </header>

  <section class="app-card p-6 sm:p-8">
    <%= render "form", bank_statement: @bank_statement %>
  </section>
</div>
